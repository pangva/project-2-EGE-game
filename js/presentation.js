const portrait=document.querySelector('#heroPortrait');
const proxy=document.querySelector('#heroAvatar');
const syncPortrait=()=>{const hero=proxy.classList.contains('nyra')?'nyra':'kael';portrait.src=`assets/images/${hero}.png`;portrait.className=hero};
new MutationObserver(syncPortrait).observe(proxy,{attributes:true,attributeFilter:['class']});
syncPortrait();
document.addEventListener('pointermove',e=>{const screen=document.querySelector('.screen.active');if(!screen||!['select','hub'].includes(screen.id))return;const x=(e.clientX/innerWidth-.5)*10,y=(e.clientY/innerHeight-.5)*6;const bg=screen.querySelector('.select-bg,.hub-scenery');if(bg)bg.style.transform=`scale(1.05) translate(${x}px,${y}px)`});
document.querySelectorAll('.hunter').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateY(${x*8}deg) rotateX(${-y*6}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
const cards=document.querySelector('#cards'),missionArt=['verb-wastes','neon-city','echo-station'];
new MutationObserver(()=>cards.querySelectorAll('.card-art').forEach((art,i)=>{art.style.backgroundImage=`linear-gradient(0deg,#07111b 3%,transparent 72%),url('assets/images/${missionArt[i]}.png')`;art.style.backgroundSize='cover';art.style.backgroundPosition='center'})).observe(cards,{childList:true});
const weapon=document.querySelector('#weaponOverlay'),weaponImage=new Image();weaponImage.src='assets/images/blaster-source.png';weaponImage.onload=()=>{weapon.width=weaponImage.width;weapon.height=weaponImage.height;const cx=weapon.getContext('2d',{willReadFrequently:true});cx.drawImage(weaponImage,0,0);const px=cx.getImageData(0,0,weapon.width,weapon.height),d=px.data;for(let i=0;i<d.length;i+=4){const r=d[i],g=d[i+1],b=d[i+2],key=g-Math.max(r,b);if(g>115&&key>45)d[i+3]=Math.max(0,255-(key-45)*5)}cx.putImageData(px,0,0)};
document.addEventListener('lexa-shot',e=>{const r=weapon.getBoundingClientRect(),sx=r.left+r.width*.5,sy=r.top+r.height*.22,dx=e.detail.x-sx,dy=e.detail.y-sy,len=Math.hypot(dx,dy),angle=Math.atan2(dy,dx)*180/Math.PI,beam=document.querySelector('#beam'),flash=document.querySelector('#muzzleFlash');beam.style.left=sx+'px';beam.style.top=sy+'px';beam.style.width=len+'px';beam.style.setProperty('--beam-transform',`rotate(${angle}deg)`);flash.style.left=sx-45+'px';flash.style.top=sy-45+'px';beam.classList.remove('fire');flash.classList.remove('fire');void beam.offsetWidth;beam.classList.add('fire');flash.classList.add('fire');weapon.animate([{transform:'translateY(0) scale(1)'},{transform:'translateY(-34px) scale(1.035)'},{transform:'translateY(-12px) scale(1.015)'},{transform:'translateY(0) scale(1)'}],{duration:260,easing:'cubic-bezier(.2,.8,.25,1)'})});
